# 蛙声科技 · AI 语音增强实习面试清单

岗位：AI 语音增强算法工程化实习（电话反馈：**比较偏模型**）  
原则：自我介绍只讲自己做过的；语音增强网络（CRN/DCCRN 等）不装熟。今晚把 **时频建模** 补到能讲清。

未发表论文细节不要在面试里展开。音频项目只讲：生成式音频训练与评测、听感指标、失真实验。

---

## 0. 定位（诚实）

介绍只讲水印里做过的：VLM 训练/推理、生成音频训练评测、Mel/时频、PESQ/SNR/MOS、压缩滤波加噪、剪枝量化。

不提语音增强、不提 CRN/DCCRN。被问岗位再答：对 AI 语音模型的训练、评测和压缩实验有兴趣。

---

## 1. 自我介绍（只讲水印，映射到 AI 语音）

约 80 秒。

> 面试官你好，我是汤惠贻，硕士在读，方向人工智能。最近主要在做生成模型上的水印，一块在视觉语言模型，一块在生成式音频。
>
> 视觉这边，我把水印做到 VLM 的训练和推理流程里，所以对 CLIP、BLIP、LLaVA 这类模型怎么训、怎么推理比较熟：视觉编码器、跨模态对齐、投影到语言模型，以及推理时怎么走前向。评测上做过微调和结构化剪枝、量化之后，主任务和水印还稳不稳。这些对音频模型同样用得上：都是 PyTorch 的训练推理管线，也都会碰到微调、剪枝、量化以后精度怎么变。
>
> 音频这边，水印是加在扩散式生成模型上的。训练和评测会用到 Mel、时频表示，指标看 SNR、PESQ、MOS；也会按弱到强做重采样、MP3 压缩、低通滤波、加噪、裁剪，看听感和可提取性还在不在。等于我比较熟音频从波形到频谱、再进网络这一套，以及失真之后怎么评。
>
> 日常用 Python 和 PyTorch，能独立把训练、对比实验和表格跑完。C++ 和 Linux 能写基础程序。希望能把这些训练、推理和音频评测的经验用到 AI 语音相关的模型工作里。谢谢。

**45 秒：**

> 我是汤惠贻，做生成模型水印。视觉上走通了 VLM 的训练和推理，也做过剪枝和量化评测；音频上在扩散生成模型里做训练评测，用 Mel 和时频特征，指标是 SNR、PESQ、MOS，并测过压缩、滤波、加噪。希望把训练推理管线和音频评测用到 AI 语音模型上。

**不要说：** 语音增强、降噪、CRN/DCCRN、端侧 NS、接得上、我想学增强。被追问岗位职责再如实说这类任务还没做过。

---

## 2. 指标对照（面试按「我用过 / 增强岗常用」分开说）

箭头：↑ 越大越好，↓ 越小越好。没跑过的指标不要说成自己的结果。

### 2.1 你音频项目里用过的

| 指标 | 方向 | 测什么 | 面试怎么说 |
| --- | --- | --- | --- |
| **SNR** | ↑ | 信号能量 / 误差能量，时域失真 | 「波形差多少」；对音量缩放敏感 |
| **PESQ** | ↑ 约 1.0–4.5 | 相对干净参考的感知质量 | 听感，生成和增强都会用；你用过 |
| **MOS** | ↑ 约 1–5 | 人打分，自然度 | 主观；你表格里有过约 4.x |
| **FAD** | ↓ | 深度特征分布距离，像 FID | **生成多样性/质量**，增强岗很少当主指标 |
| **CLAP-Score** | ↑ | 音频–文本语义是否还对齐 | 生成语义；增强岗一般不看 |
| **ACC / VSR / FPR** | ACC/VSR↑ FPR↓ | 水印能否检出、误报 | 版权任务专用，介绍里可一笔带过 |
| 比特恢复准确率 | ↑ | 失真后载荷还能不能对上 | 鲁棒性实验用，不是降噪指标 |

竞赛里还分过攻击强度，中等攻击下比特恢复大约 **91.6%**（只有被问鲁棒性实验时再说）。

### 2.2 语音增强岗更认的（今晚要能解释，未做过就说「在补」）

| 指标 | 方向 | 测什么 | 和你用过的关系 |
| --- | --- | --- | --- |
| **SI-SDR** | ↑ | 去缩放、去平移后的失真 | 比 SNR 更公平；增强主指标之一，你还没用过 |
| **STOI** | ↑ 0–1 | 可懂度（字清不清） | PESQ 偏「好不好听」，STOI 偏「听不听得清」 |
| **PESQ** | ↑ | 感知质量 | **两边共用**，这是你最能开口的 |
| **DNSMOS** | ↑ | 无参考听感（DNS Challenge） | 没有干净参考时用；你还没用过 |
| **RTF** | ↓ 要 < 1 | 处理时长 / 音频时长 | 端侧实时；研究项目通常没报 |
| 算法延迟 | ↓ | 窗长、帧移、lookahead | 和 STFT 参数绑在一起 |

### 2.3 时频 / DSP 本身对应的量（不是「模型分数」，是参数和物理量）

| 量 | 对应哪一步 | 记住什么 |
| --- | --- | --- |
| 采样率 \(f_s\) | 波形 | 16 kHz 常见；奈奎斯特 \(f_s/2\) |
| 窗长、hop、重叠率 | STFT | 频率分辨率 vs 时间分辨率 vs 延迟 |
| 幅度 \|X\|、相位 ∠X | 每个频率点 | 响度看幅度；还原常要相位 |
| dB | \(20\log_{10}\|X\|\) | 把动态范围压到好算 |
| Mel 维数 | 滤波器组 | 64/80/128；感知压缩，细节少于线性谱 |
| 截止频率 | 低通/高通 | 例如 3 kHz 低通会切掉大量齿音 |
| 码率 kbps | MP3 压缩 | 64 / 32 / 8 kbps 越低伤得越重 |
| 加噪 SNR（dB） | 高斯噪声攻击 | **这里的 SNR 是「加多大噪」**，和评测里的 SNR 不是同一个实验步骤 |

口答一句：评测 SNR/PESQ 是「输出好不好」；攻击里写 30 dB 高斯噪声是「输入被破坏成什么样」。

### 2.4 视觉项目里报过的（被问压缩时用）

水印成功率、主任务精度、MSE / PSNR / SSIM；剪枝比例、量化（FP16 / INT8）。不要说成语音指标。

---

## 3. 音频攻击：列全 + 怎么做

你项目里是 **对已生成/已嵌入的音频做失真，再测指标还在不在**（听感 + 能否恢复信息）。增强岗的加噪是 **造训练数据**。做法类似，目的不同，介绍时分开说。

实验时统一：读 wav → 变成单声道、对齐采样率 → 做攻击 → 再重采样回模型采样率（你这边曾用 22.05 kHz）→ 算 PESQ/SNR 或提取准确率。可用 `torchaudio`、`librosa`、`ffmpeg`。

### 3.1 信号失真类（黑盒、最常问）

**1）MP3 / 有损压缩**

- 干什么：丢掉人耳不敏感的频谱，再解压成 wav，波形和相位都变。
- 怎么做：`ffmpeg -i in.wav -codec:a libmp3lame -b:a 64k tmp.mp3`，再解码回 wav。码率越低伤越重。
- 你用过的档：约 **64 / 32 / 8 kbps**（弱 / 中 / 强）。
- 对应指标：PESQ、SNR 会掉；载荷比特准确率也会掉。

**2）重采样**

- 干什么：先降采样再升回来，高于新奈奎斯特的频率没了（还有抗混叠滤波）。
- 怎么做：例如 22.05 kHz → **16 kHz** → 再回到 22.05 kHz（`resample`，sinc 滤波）。
- 对应：高频辅音变糊；PESQ 轻伤。

**3）音量 / 幅度**

- 干什么：乘增益或峰值归一化。有的指标（普通 SNR）会被音量干扰，PESQ 相对稳一些。
- 怎么做：峰值归一化到 1，或随机增益 ±若干 dB，注意防削波（clip）。
- 你弱攻击里用过 **音量归一化**。

**4）低通滤波**

- 干什么：挡高频。齿音、谐波细节没了。
- 怎么做：Butterworth / FIR 低通，截止约 **3000 Hz**（中等攻击）。`torchaudio.functional.lowpass_biquad` 或 scipy `butter` + `filtfilt`（非实时可用双边滤波；实时只能因果）。
- 对应：高频能量没了，PESQ/可懂度下降。

**5）加性高斯噪声**

- 干什么：\(y = x + n\)，\(n\sim\mathcal N(0,\sigma^2)\)，\(\sigma\) 由目标 SNR 反推。
- 怎么做：算信号功率 \(P_x\)，要 SNR = 30 dB 则 \(P_n = P_x / 10^{30/10}\)，再采样噪声加上去。
- 你中等攻击用过 **30 dB 高斯噪声**（数字越大噪越小）。
- 对应评测：输出 SNR/PESQ；增强岗训练时也这样 **合成带噪数据**，SNR 常在 -5~20 dB 随机抽。

**6）裁剪**

- 干什么：时间上丢掉一段，或只留中间一段。
- 怎么做：随机起点切开，丢掉 **50%**（强）或 **90%**（极端）；评比特时要对齐剩下的部分或允许部分丢失。
- 对应：时域对齐被破坏，对「铺在时间上」的信息更狠。

**7）复合攻击**

- 干什么：压缩 + 滤波 + 噪声串起来，模拟平台转码。
- 怎么做：按固定顺序管线，例如 MP3 8 kbps → 低通 → 再加噪；或 **8 kbps + 裁 90%**（极端）。
- 对应：单看一项 PESQ 不够，要看最差组合。

### 3.2 按强度记忆（方便口述）

| 档 | 你实验里怎么组合 | 模拟什么 |
| --- | --- | --- |
| 弱 | 16 kHz 重采样、MP3 64 kbps、音量归一化 | 平台常规转码、预处理 |
| 中 | MP3 32 kbps、3 kHz 低通、30 dB 高斯噪声 | 有意滤波、降噪、轻处理 |
| 强 | MP3 8 kbps、随机裁 50%、压缩+滤波+噪声 | 低码率 + 主动破坏 |
| 极端 | 裁 90%，或 8 kbps 再裁 90% | 几乎只剩碎片 |

口述：弱是转码，中是滤波加噪，强是狠压缩再复合，极端是大段裁掉。

### 3.3 生成链路类（知道名字即可，实现别展开）

再生成、声码器重合成、去噪器再滤一遍：音频被另一个模型重写，频谱对不齐。面试说「相当于又编解码一次」即可，不必讲你论文里怎么防。

### 3.4 模型侧（视觉项目为主，音频白盒评过同类）

| 攻击 | 怎么做 | 看什么 |
| --- | --- | --- |
| 清洁数据微调 | 用一部分干净数据再训若干 epoch | 主任务还在不在、水印/行为还在不在 |
| 结构化剪枝 | 按通道/滤波器幅度裁掉 10%–30% | 是否还能触发或检出 |
| 量化 | FP16；INT8 + 轻微权重噪声 | 精度–体积；端侧 NS 也问这个 |

这里的「攻击」是改 **模型权重**，和上面改 **波形** 不是一类。被问就主动分开。

### 3.5 和语音增强的差别（被问「你做的攻击不就是降噪反面吗」）

- 你：先有一条音频，再破坏它，看质量和可提取性掉多少。
- 增强训练：故意加噪做成 \(y=s+n\)，网络要还原 \(s\)，主指标是 SI-SDR/PESQ/STOI。
- 可以说：加噪、滤波、压缩的 **实现** 我会；增强是换目标去还原，网络我还没训过。

---

## 4. 今晚重点：时频建模（建议先学这块）

目标：能用自己的话讲「波形怎么变成频谱、Mel 是什么、相位是什么」。下面按顺序看，每条能讲 30 秒再打勾。

### 2.1 波形（时域）

- [ ] 声音在计算机里是 **一维序列** \(x[n]\)，每个点是振幅。
- [ ] **采样率 \(f_s\)**：每秒多少个点。电话/很多语音模型 16 kHz；你项目里常见 22.05 kHz 或 48 kHz。
- [ ] **奈奎斯特：** 能表示的最高频率大约是 \(f_s/2\)。16 kHz 采样，最高约 8 kHz。
- [ ] 语音：浊音有基频 F0（音高）和共振峰；清音/辅音能量更散、更靠高频。所以乱滤高频会「口齿不清」。

### 2.2 为什么要时频

- [ ] 纯时域：看波形，噪声和语音叠在一起，难分开。
- [ ] 纯 FFT 整段：只有一张总频谱，**没有时间变化**（谁先说话、噪声何时出现都不知道）。
- [ ] **时频：** 把信号切成短帧，每帧做频谱 → 横轴时间、纵轴频率，这就是语谱图。增强、识别、很多生成模型都在这张图上干活。

### 2.3 STFT（短时傅里叶变换）——今晚核心

一帧的做法：取出一段波形 → 乘窗 → FFT → 得到这一帧各个频率的复数。

要记住的三个参数：

| 参数 | 含义 | 变大了会怎样 |
| --- | --- | --- |
| 窗长 / `n_fft` / `win_length` | 一帧覆盖多长时间 | 频率更细，时间更糊，延迟更大 |
| 帧移 / `hop_length` | 下一帧跳多远 |  hop 大：帧少、算得快、时间更稀、延迟相关 |
| 窗函数（常用 Hann） | 避免截断造成频谱泄漏 | 一般别用矩形窗 |

- [ ] 常见数量级：16 kHz 时，窗长 20–32 ms（约 320–512 点），hop 10 ms（约 160 点），重叠 50% 很常见。
- [ ] **OLA（重叠相加）：** 还原波形时把各帧加回去，重叠是为了接得平滑。
- [ ] **ISTFT：** 频谱变回波形。训练里若损失在频谱上，有时还要 ISTFT 再听。

**口答：** 窗长换频率分辨率，帧移换时间分辨率；实时系统 hop 不能太大也不能为了细频率把窗拉得很长，否则延迟高。

### 2.4 幅度、相位、能量、dB

FFT 每个频率点是 **复数** \(a+bi\)：

- [ ] **幅度** \(|X|=\sqrt{a^2+b^2}\)：这一频率有多「响」，听感主要看它。
- [ ] **相位** \(\angle X\)：正弦从哪一时刻开始。单独看相位图很难懂，但还原波形必须要相位（或用别的办法估）。
- [ ] **功率/能量：** 幅度的平方。
- [ ] **dB：** \(20\log_{10}|X|\)（幅度）或 \(10\log_{10}\)（功率），把很大的动态范围压到好画、好算的尺度。
- [ ] **对数幅度 / log-mel：** 生成模型常用，更接近人耳对响度的感觉。

很多降噪会：估一个更好的幅度，相位先继续用带噪的（省事、能实时）。质量要求高再估复谱或直接时域。你能说到这一层就够，不必说自己做过。

### 2.5 Mel 频谱（你项目里用过，要能解释）

- [ ] 人耳对低频更敏感：同样是 100 Hz 的差别，在低音区比高音区更容易分辨。
- [ ] **Mel 刻度：** 把 Hz 非线性地弯一下，低频分得细、高频分得粗。
- [ ] **Mel 滤波器组：** 在 STFT 幅度上乘一组三角滤波，得到 Mel 频带能量，再常取 log → **log-mel**。
- [ ] 维数通常 64/80/128，远小于 FFT 点数（如 513），所以省计算、适合当神经网络输入。
- [ ] **局限：** Mel 已经丢掉一部分频率细节和相位；**语音增强要精细恢复谐波和辅音时，常用线性频谱或波形，不一定 Mel。** 生成/TTS 更爱 Mel。

被问「你用的 Mel 和降噪频谱一样吗」：

> 我训练生成模型时主要用 Mel 或模型自己的时频特征。增强文献里也常用线性 STFT 或波形。Mel 是感知压缩后的，细节更少。具体用哪种我还在学。

### 2.6 滤波器（JD 要求，用直觉即可）

- [ ] **低通：** 放低频、挡高频。去高频嘶声，但可能伤齿音（s、sh）。
- [ ] **高通：** 去低频隆隆声、风噪的一部分，可能伤男声胸腔感。
- [ ] **带通：** 只留中间一段（比如电话频带）。
- [ ] **陷波：** 去掉某一个嗡嗡频点（电流声 50 Hz 等）。
- [ ] FIR / IIR：FIR 更稳、可做成线性相位；IIR 更省，但要小心不稳定。面试能说「滤波就是在频率上加权」即可。

你项目里「滤波当攻击」：可以理解为频谱被改过，模型还能不能工作；不要说成自己设计过滤波器。

### 2.7 时频图上，模型在干什么（建立直觉，不对应你做过 NS）

- [ ] 语谱图可以当成「时间 × 频率」的二维图，CNN/U-Net 会在这上面滑。
- [ ] 语音谐波在语谱图上是横着随时间走的一条条纹；噪声常常更散。
- [ ] 增强的一种常见想法：学一个 **mask（0~1 的权重）** 乘在带噪频谱上，把噪声格子压小、语音格子留下。这是在读论文，不是我的项目经验。

### 2.8 今晚时频自测（闭卷）

1. 采样率 16 kHz，理论上最高频率大约多少？
2. 为什么整段 FFT 不够，要用 STFT？
3. 窗长变长，频率和时间分辨率谁更好、谁更差？
4. 幅度和相位哪个更影响「响不响」，还原波形能不能不要相位？
5. Mel 相对线性频谱，低频和高频谁分得更细？增强为什么不一定用 Mel？
6. hop 变大，计算量和时间轴密度怎么变？
7. 低通滤波可能伤语音的哪类音？
8. dB 是在做什么？

答案要点：1）8 kHz；2）要时间变化；3）频率更好、时间更差；4）响度看幅度，还原一般要相位或替代估计；5）低频更细，增强常要更细的线性谱/波形；6）算得少、时间更稀；7）高频辅音/齿音；8）把幅度压到对数尺度。

---

## 5. 语音增强：只记任务，不装会模型

- [ ] 输入带噪 \(y\)，输出干净语音估计 \(\hat{s}\)；最常见 \(y=s+n\)。
- [ ] 和 TTS（无中生有说话）、分离（两人叠在一起）不同。
- [ ] 通话往往要 **因果**（不能看未来很多帧）、看延迟和 RTF。
- [ ] 评测名字：SI-SDR、PESQ、STOI；你用过 PESQ/SNR/MOS，可以说「PESQ 我在生成音频里用过，STOI/SI-SDR 是增强里更常见的可懂度和失真指标，我还在补」。
- [ ] CRN、DCCRN、NSNet：今晚有余力再看结构图；**介绍里不要主动报这些名字。** 被问就说：还在看频谱 mask、时域波形两类思路，没有自己训过。

量化 / 剪枝 / 蒸馏：只在被问视觉项目时讲「做过量化剪枝后指标变化」。不要主动说「所以我会做端侧 NS 压缩」。

---

## 6. 项目 40 秒（被追问）

**音频：** 给生成音频做可验证信息，尽量不影响听感。评测用 SNR、PESQ、MOS；鲁棒性按弱到极端做重采样、MP3、音量归一化、3 kHz 低通、高斯噪声、裁剪和复合攻击。频谱特征主要是 Mel。增强网络不是这个项目里的内容。

**视觉：** CLIP/BLIP/LLaVA 上训练和验证，看过微调、剪枝、量化后的稳定性。用来说明我会跑深度学习和消融，不往降噪上靠。

**为什么投这个实习（被问再答，介绍里不主动提增强）：** 我在做生成音频和 VLM 水印，熟训练推理、时频和听感评测。AI 语音同样是模型训练、推理和失真/压缩评测，我想把这套用在语音相关模型上。

---

## 7. 今晚时间表

| 时间 | 做什么 |
| --- | --- |
| 40 min | 第 2 节指标 + 第 3 节攻击（能口述弱/中/强怎么做） |
| 50 min | 第 4 节时频：STFT、幅度相位、Mel，做完自测 |
| 15 min | 自我介绍念两遍 |
| 余力 | 第 5 节增强任务；SI-SDR vs PESQ vs STOI |

睡前一张纸：PESQ/SNR/MOS 是你用过的；SI-SDR/STOI 是增强常用；攻击四档（转码 / 低通+加噪 / 狠压缩+复合 / 大裁剪）。
